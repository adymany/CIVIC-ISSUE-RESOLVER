/**
 * @type {import('next').NextConfig}
 */
const nextConfig = {
  // Specify the root directory to avoid workspace detection warnings
  turbopack: {
    root: "C:\\Users\\adnan\\Desktop\\New folder\\civic-reporting-system"
  }
};

module.exports = nextConfig;